<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Todo App</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            font-family: "Segoe UI", Tahoma, sans-serif;
            background: linear-gradient(135deg, #e3f2fd, #ffffff);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }

        /* Main container */
        .todo-container {
            background: #ffffff;
            width: 900px;
            max-width: 95%;
            padding: 24px;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.12);
        }

        /* Heading */
        .todo-container h2 {
            text-align: center;
            margin-bottom: 10px;
            color: #333;
            letter-spacing: 0.5px;
        }

        /* Top action bar */
        .top-actions {
            display: flex;
            justify-content: flex-start;
            margin-bottom: 14px;
        }

        /* Home button */
        .home-btn {
            padding: 6px 16px;
            background: #198754;
            color: #ffffff;
            border: none;
            border-radius: 8px;
            font-size: 13px;
            cursor: pointer;
            transition: background 0.2s ease, transform 0.2s ease;
        }

        .home-btn:hover {
            background: #146c43;
            transform: scale(1.05);
        }

        /* Grid layout for tasks */
        #tasks {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
            gap: 10px;
        }

        /* Individual task card */
        .task {
            background: #f8f9fa;
            padding: 10px 12px;
            border-left: 5px solid #28a745;
            border-radius: 10px;
            font-size: 14px;
            color: #333;
            line-height: 1.4;
        }

        .task strong {
            font-weight: 600;
        }

        /* Hover */
        .task:hover {
            background: #eef7f1;
            transform: translateY(-2px);
            transition: 0.2s ease;
        }

        /* Buttons inside task */
        .task button {
            margin-top: 8px;
            padding: 6px 14px;
            background: #0d6efd;
            color: #ffffff;
            border: none;
            border-radius: 6px;
            font-size: 13px;
            cursor: pointer;
            transition: background 0.2s ease, transform 0.2s ease;
        }

        .task button:hover {
            background: #084298;
            transform: scale(1.05);
        }

        /* Remove underline from links */
        .task a {
            text-decoration: none;
        }
    </style>
</head>

<body>

<div class="todo-container">
    <h2>Todo List</h2>

    <!-- Home Button -->
    <div class="top-actions">
           <a href="/addone"> <button class="home-btn">🏠 Home</button></a>
    </div>

    <!-- Tasks -->
    <div id="tasks">
        <% data.forEach(item => { %>
            <div class="task">
                <div><strong>📝 Task:</strong> <%= item.task %></div>
                <div><strong>📅 Date:</strong> <%= item.date %></div>

                <% if (item.status === "completed") { %>
                    <div><strong>📈 Status:</strong> Completed ✅</div>
                <% } else { %>
                    <div><strong>📈 Status:</strong> Pending..</div>
                <% } %>

                <a href="/edit/<%= item._id %>"><button>Edit</button></a>
                <a href="/task/delete/<%= item._id %>"><button>Delete</button></a>
            </div>
        <% }) %>
    </div>
</div>

</body>
</html>
