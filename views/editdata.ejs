<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Todo App</title>

    <style>
       /* Reset */
* {
    box-sizing: border-box;
}

/* Page */
body {
    font-family: "Segoe UI", Tahoma, sans-serif;
    background: linear-gradient(135deg, #e3f2fd, #ffffff);
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    margin: 0;
}

/* Main container */
.todo-container {
    background: #ffffff;
    width: 900px;
    max-width: 95%;
    padding: 30px;
    border-radius: 18px;
    box-shadow: 0 12px 35px rgba(0,0,0,0.15);
}

/* Heading */
.todo-container h2 {
    text-align: center;
    margin-bottom: 30px;
    color: #2e7d32; /* green */
    letter-spacing: 0.6px;
}

/* Center the form */
#tasks {
    display: flex;
    justify-content: center;
    align-items: center;
}

/* Form card */
#tasks form {
    background: #f8f9fa;
    padding: 25px 30px;
    border-radius: 14px;
    width: 100%;
    max-width: 420px;
    box-shadow: 0 8px 20px rgba(0,0,0,0.1);
}

/* Labels */
#tasks label {
    font-weight: 600;
    color: #333;
    display: block;
    margin-bottom: 6px;
}

/* Inputs */
#tasks input[type="text"],
#tasks input[type="date"] {
    width: 100%;
    padding: 10px 12px;
    border-radius: 8px;
    border: 1px solid #ccc;
    margin-bottom: 18px;
    font-size: 14px;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
}

/* Input focus */
#tasks input:focus {
    outline: none;
    border-color: #28a745;
    box-shadow: 0 0 0 2px rgba(40, 167, 69, 0.2);
}

/* Green button */
#tasks button {
    width: 100%;
    padding: 12px;
    background: #28a745;       /* green */
    color: #ffffff;
    border: none;
    border-radius: 8px;
    font-size: 15px;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.2s ease, transform 0.2s ease, box-shadow 0.2s ease;
}

/* Button hover */
#tasks button:hover {
    background: #1e7e34;
    transform: translateY(-2px);
    box-shadow: 0 6px 14px rgba(40, 167, 69, 0.35);
}

/* Button active */
#tasks button:active {
    transform: scale(0.98);
}


    </style>
</head>

<body>

<div class="todo-container">
    <h2>Todo List</h2>

    <div id="tasks">
      <form action="/accept/data/<%=data._id%>" method="POST">
        <label for="task">Task:</label><br>
        <input type="text" id="task" value="<%=data.task%>" name="task"><br>
        <label for="startdate">Date</label><br>
        <input type="date" id="startdate" value="<%=data.date%>" name="date"><br>
        <label for="completeness" >Status:(complete or not)</label>
        <input type="checkbox" id="completeness" name="status" value="completed"><br>
        <button value="submit">Update</button>
      </form>
    </div>
</div>

</body>
</html>
