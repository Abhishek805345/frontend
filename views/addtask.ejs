<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Todo Dashboard</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Inter", "Segoe UI", sans-serif;
    }

    body {
      background: #f7f8fa;
      min-height: 100vh;
      display: flex;
    }

    /* ================= SIDEBAR ================= */
    .sidebar {
      width: 230px;
      background: #ffffff;
      border-right: 1px solid #e5e7eb;
      padding: 20px;
      flex-shrink: 0;
    }

    .logo {
      font-size: 20px;
      font-weight: 700;
      color: #6c5ce7;
      margin-bottom: 30px;
    }

    .menu a {
      display: block;
      padding: 12px 14px;
      margin-bottom: 8px;
      text-decoration: none;
      color: #333;
      border-radius: 8px;
      font-size: 14px;
    }

    .menu a.active,
    .menu a:hover {
      background: #f0edff;
      color: #6c5ce7;
      font-weight: 600;
    }

    /* ================= MAIN ================= */
    .main {
      flex: 1;
      padding: 30px 40px;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

.header {
  width: 100%;
  max-width: 520px;   /* SAME as card */
  margin: 0 auto 30px;
  text-align: center;
}

.greeting {
  font-size: 14px;
  color: #6b7280; /* soft gray */
  margin-bottom: 6px;
}

.title {
  font-size: 26px;
  font-weight: 600;
  color: #111827;
}



    .card {
      background: #ffffff;
      border-radius: 14px;
      padding: 25px;
      width: 100%;
      max-width: 520px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.08);
    }

    .card h2 {
      font-size: 16px;
      margin-bottom: 15px;
      color: #333;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 14px;
    }

    input[type="text"],
    input[type="date"] {
      padding: 12px 14px;
      border-radius: 10px;
      border: 1px solid #d1d5db;
      font-size: 14px;
      outline: none;
      width: 100%;
    }

    input:focus {
      border-color: #6c5ce7;
      box-shadow: 0 0 0 3px rgba(108,92,231,0.15);
    }

    .btn-group {
      display: flex;
      gap: 12px;
      margin-top: 10px;
    }

    button {
      flex: 1;
      padding: 12px;
      border: none;
      border-radius: 10px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      transition: transform 0.2s ease, background 0.2s ease;
    }

    .btn-add {
      background: #6c5ce7;
      color: white;
    }

    .btn-add:hover {
      background: #5848c2;
      transform: translateY(-2px);
    }

    /* ================= RESPONSIVE ================= */
    @media (max-width: 900px) {
      body {
        flex-direction: column;
      }

      .sidebar {
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: space-between;
        border-right: none;
        border-bottom: 1px solid #e5e7eb;
      }

      .menu {
        display: flex;
        gap: 10px;
      }

      .menu a {
        margin-bottom: 0;
        padding: 8px 10px;
        font-size: 13px;
      }

      .main {
        padding: 20px;
      }

      .header h1 {
        text-align: center;
        font-size: 20px;
      }
    }

    @media (max-width: 480px) {
      .card {
        padding: 20px;
      }

      .btn-group {
        flex-direction: column;
      }

      button {
        width: 100%;
      }
    }
  </style>
</head>

<body>

  <!-- SIDEBAR -->
  <div class="sidebar">
    <div class="logo">TodoSpace</div>
    <div class="menu">
      <a href="#" class="active">Add Tasks</a>
      <a href="/getdata">Tasks</a>
      <a href="/completed">Completed</a>

    </div>
  </div>

  <!-- MAIN -->
  <div class="main">
   <div class="header">
  <p class="greeting">Hey Abhishek 👋</p>
  <h1 class="title">What are you working on?</h1>
</div>

    <div class="card">
      <h2>Add New Task</h2>

      <form action="/data" method="POST">
        <input
          type="text"
          name="task"
          placeholder="What are you working on?"
          required
        />

        <input type="date" name="date" required />

        <input type="hidden" value="<%=data.id%>" name="userid" />

        <div class="btn-group">
          <button type="submit" class="btn-add">Add Task</button>
        </div>
      </form>
    </div>
  </div>

</body>
</html>
